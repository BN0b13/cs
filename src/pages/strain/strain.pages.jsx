import React from 'react';

import Welcome from '../../components/welcome/welcome.component';

import './age-verify.styles.scss';

const Strain = ({ currentHomeDisplay }) => {
    return (
        <h1>Strain Display</h1>
    );
};

export default Strain;